gym env
	gat_state 观测状态输出标志: 1. encoder处理 2. 待分配任务的状态
	encoder 模型(使用模型, 不使用权重)
	
	周期缩短率 (  )
	
	simulate env
		创建IoT和MEC对象,同时创建任务, 那么这个时候就需要将
	
	
	
关于如何开启保存gv图的代码
def cast_to_gv(tasks: "List[Task]", cast_flag=True): # 将cast_flag标志打开即可
